export interface ListFilterFormInterface {
  position: number | null;
  modules: number | null;
  institution: string | null;
  district_id: number | null;
  region_id: number | null;
  page?: number | null;
  per_page?: number | null;
}
export interface headData {
  name: string;
}
